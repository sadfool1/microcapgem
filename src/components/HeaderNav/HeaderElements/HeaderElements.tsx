import React, { Fragment } from 'react';
import {HeaderElementProps} from "../../../interface/HeaderNav/HeaderElements"

const HeaderElements = (props: HeaderElementProps) => {
    const data = { props }
    return (
        <Fragment>
            <div className=''>

            </div>
        </Fragment>
    );  
}

export default HeaderElements;